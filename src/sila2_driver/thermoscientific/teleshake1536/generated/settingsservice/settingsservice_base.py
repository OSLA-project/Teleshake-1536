# Generated by sila2.code_generator; sila2.__version__: 0.12.2
from __future__ import annotations

from abc import ABC, abstractmethod
from typing import TYPE_CHECKING

from sila2.server import FeatureImplementationBase, MetadataDict

from .settingsservice_types import SetSerialPort_Responses

if TYPE_CHECKING:
    from typing import TypeVar

    from sila2.server import SilaServer

    Server = TypeVar("Server", bound=SilaServer)


class SettingsServiceBase(FeatureImplementationBase, ABC):
    parent_server: Server

    def __init__(self, parent_server: Server):
        """
        Feature to handle settings
        """
        super().__init__(parent_server=parent_server)

    @abstractmethod
    def get_SerialPort(self, *, metadata: MetadataDict) -> str:
        """
        Serial port used

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Serial port used
        """

    @abstractmethod
    def SetSerialPort(
        self, PortName: str, *, metadata: MetadataDict
    ) -> SetSerialPort_Responses:
        """
        Set serial port to use


        :param PortName: Port name, e.g. COM3 or /dev/ttyS1

        :param metadata: The SiLA Client Metadata attached to the call

        """
