# Generated by sila2.code_generator; sila2.__version__: 0.12.2
from __future__ import annotations

from abc import ABC, abstractmethod
from datetime import timedelta
from typing import TYPE_CHECKING, Optional

from sila2.server import (
    FeatureImplementationBase,
    MetadataDict,
    ObservableCommandInstanceWithIntermediateResponses,
)

from .shakecontroller_types import (
    GoHome_Responses,
    LockPlate_Responses,
    ShakeForTime_IntermediateResponses,
    ShakeForTime_Responses,
    StartShaking_Responses,
    StopShaking_Responses,
    UnlockPlate_Responses,
)

if TYPE_CHECKING:
    from typing import TypeVar

    from sila2.server import SilaServer

    Server = TypeVar("Server", bound=SilaServer)


class ShakeControllerBase(FeatureImplementationBase, ABC):
    parent_server: Server

    ShakeForTime_default_lifetime_of_execution: Optional[timedelta]

    def __init__(self, parent_server: Server):
        """
        Features to control shaking
        """
        super().__init__(parent_server=parent_server)

        self.ShakeForTime_default_lifetime_of_execution = None

    @abstractmethod
    def StopShaking(
        self, ShakerId: int, *, metadata: MetadataDict
    ) -> StopShaking_Responses:
        """
        Stop shaking


        :param ShakerId: ID of the target shaker in the daisy chain. Use 0 to target the directly connected shaker.

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def StartShaking(
        self,
        ShakerId: int,
        TargetSpeed: float,
        TargetPower: float,
        *,
        metadata: MetadataDict,
    ) -> StartShaking_Responses:
        """
        Run the shaker for an undefined amount of time with the requested speed and power.


        :param ShakerId: ID of the target shaker in the daisy chain. Use 0 to target the directly connected shaker.

        :param TargetSpeed: Target speed in rpm

        :param TargetPower: Target power in percent

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def GoHome(self, ShakerId: int, *, metadata: MetadataDict) -> GoHome_Responses:
        """
        Move the plate to home position.


        :param ShakerId: ID of the target shaker in the daisy chain. Use 0 to target the directly connected shaker.

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def UnlockPlate(
        self, ShakerId: int, *, metadata: MetadataDict
    ) -> UnlockPlate_Responses:
        """
        Unlock plate


        :param ShakerId: ID of the target shaker in the daisy chain. Use 0 to target the directly connected shaker.

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def LockPlate(
        self, ShakerId: int, *, metadata: MetadataDict
    ) -> LockPlate_Responses:
        """
        Lock the plate


        :param ShakerId: ID of the target shaker in the daisy chain. Use 0 to target the directly connected shaker.

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def ShakeForTime(
        self,
        ShakerId: int,
        Runtime: int,
        TargetSpeed: float,
        TargetPower: float,
        *,
        metadata: MetadataDict,
        instance: ObservableCommandInstanceWithIntermediateResponses[
            ShakeForTime_IntermediateResponses
        ],
    ) -> ShakeForTime_Responses:
        """
        Run the shaker for a defined runtime with the requested speed and power.


        :param ShakerId: ID of the target shaker in the daisy chain. Use 0 to target the directly connected shaker.

        :param Runtime: Runtime in seconds

        :param TargetSpeed: Target speed in rpm

        :param TargetPower: Target power in percent

        :param metadata: The SiLA Client Metadata attached to the call
        :param instance: The command instance, enabling sending status updates to subscribed clients

        """
